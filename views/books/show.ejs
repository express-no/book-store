<h1><%= book.title %></h1>

<div class="">
  <img src="<%= book.coverImagePath %>" width="100" height="100" />
  <button><a href="/books/<%= book.id %>/edit">Edit</a></button>
  <%- include("../partials/deleteForm",{url: `/books/${book.id}`}) %>
  <button><a href="/authors/<%= book.author.id %>">View Author</a></button>
</div>

<div class="">
  <div>Author: <%= book.author.name %></div>
  <div>Publish Date: <%= book.publishDate.toDateString() %></div>
  <div>Page Count: <%= book.pageCount %></div>
  <div>Discription: <%= book.description %></div>
</div>

<div></div>
